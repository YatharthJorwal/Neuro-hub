# new-session-prompt.md

Template for starting a new session (chat, Claude Code, whatever) on
this project from scratch. Fill in the bracketed part each time — don't
paste this generic, the whole point is saying what this specific
session is for.

---

Clone `https://github.com/YatharthJorwal/Neuro-hub` and read `CLAUDE.md`
first — it's short on purpose and points to `handoff.md`,
`architecture.md`, `decisions.md`, and `plans.md` for everything else.
Read `handoff.md` next; it has an explicit staleness warning at the
top — if what you find in the repo doesn't match it, trust the repo and
flag the mismatch before doing anything else, don't just proceed on the
stale version.

Don't assume anything about this project beyond what those files say.
Check `decisions.md` before re-deciding anything already settled there.

When delivering file changes this session, follow the Workflow section
in `CLAUDE.md` — exact apply commands with full absolute paths, not a
bare zip.

[What to actually do this session — e.g. "build the real local
embedder" / "start the chat layer" / "work on real FlyWire connectome
data in the visualization." Be specific — a new session can't infer
this part.]
